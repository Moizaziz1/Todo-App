"""
Schema definitions for task-related API requests and responses
"""
from pydantic import BaseModel
from typing import Optional
from datetime import datetime


# Already defined in models/task.py, so this file will contain additional schemas if needed
# For now, we'll leave it with a comment indicating that the core schemas are in models